import { QueryClient } from 'react-query';
// import { renderHook } from '@testing-library/react-hooks';

// const createWrapper = () => {
//     const queryCnew QueryClient
// }

test('useGetUserProfile: return transformed data', () => {});
